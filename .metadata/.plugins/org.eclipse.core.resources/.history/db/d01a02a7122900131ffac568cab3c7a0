#include <stdio.h>
#include "main.h"
#include <Print.h>

size_t printHexString(Print & pr, uint8_t * a, int n) {
	size_t i;
    for(i = 0; i < n; i++) {
      pr.print(a[i]>>4, HEX);
      pr.print(a[i]& 0x0f, HEX);
      pr.print(' ');
    }
    return i*3;
}
