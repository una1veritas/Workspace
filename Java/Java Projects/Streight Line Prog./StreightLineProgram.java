import java.util.*;class Unary {	char literal;			Unary(char c) {		literal = c;	}		public boolean isUnary() {		return true;	}		public boolean equals(Object o) {		if ( o instanceof Unary ) 			return literal == ((Unary) o).literal;		return false;	}	public synchronized String toString() {		return (new StringBuffer(literal)).toString();	}}class Binary extends Unary {	Binary first, last;		Binary(Binary f, Binary l) {		super('\0');		first = f;		last = l;	}		public boolean isUnary() {		return false;	}		public boolean equals(Object o) {		if ( o instanceof Binary ) 			return first == ((Binary) o).first && last == ((Binary) o).last;		return false;	}		public synchronized String toString() {		return first.toString() + last.toString();	}	}class StreightLineProgram {	Binary root;		StreightLineProgram(String str) {		// Obvious one			}}